# Public source publication — 5 September 2026

## Repository and history

Public repository: [ajayasai/OpenBumpPlan](https://github.com/ajayasai/OpenBumpPlan).

The user created the repository. Publication preserved its initial commit `aebe977fa6605bdb081a4cb24b134f0aa4e137ed` and appended commits without force-pushing.

The complete application and reproduced deliverables were committed in [950f01df213a3d9a3a6741c8e621d6a8a7de59ac](https://github.com/ajayasai/OpenBumpPlan/commit/950f01df213a3d9a3a6741c8e621d6a8a7de59ac). Subsequent documentation and CI setup do not change the application code.

## GitHub-hosted verification

[Publication run 33962178905](https://github.com/ajayasai/OpenBumpPlan/actions/runs/33962178905) completed successfully. Its job verified and unpacked the prepared source, ran the engine tests, reproduced the standalone build and exported examples, installed Chromium, ran the browser scenarios, and committed the release files.

- Node tests: **151 passed, zero failed**.
- Browser workflows: **12 passed, zero failed**, Chromium **151.0.7922.34**.
- Offline build: SHA-256 exactly matched the original tested build.
- Demo and optimized example: CLI validation passed their configured acceptance policies.
- SVG, PDF, HTML, and Markdown example reports were regenerated by the CLI.
- The studio screenshot was captured from the running application.

The new browser report is [github-browser-test-results.json](github-browser-test-results.json). The original local records remain in [browser-test-results.json](browser-test-results.json) and [VALIDATION.md](VALIDATION.md).

The browser harness loads the actual standalone HTML through Playwright `set_content`, using fresh contexts. Native file/localhost navigation and real-origin storage persistence are not tested by that harness. Its generated limitation text refers to restrictions observed in the original local environment; it does not establish that GitHub-hosted Chromium has the same navigation restriction.

## Reproducibility identifiers

Prepared source transfer archive, SHA-256:

```text
c2609ed9e90428b10cb64bf5cb05157643b0f58a0f144e9b44dcf450cabf9483
```

Standalone HTML, SHA-256:

```text
be83a745173d6af90e64ea370dc9787ffa4bedb748bf826aabf369667a2a7d75
```

The temporary transfer files were removed after extraction, and the one-time contents-writing import workflow was removed after successful publication. Ongoing engine CI has read-only repository permissions. The optional Pages workflow deploys only when manually invoked after Pages is configured.

Public source publication is not a hosted-demo deployment, electrical signoff, security certification, or evidence of superiority over commercial EDA tools.
